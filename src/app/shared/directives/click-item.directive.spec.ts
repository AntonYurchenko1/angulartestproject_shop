import { ClickItemDirective } from './click-item.directive';

describe('ClickItemDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickItemDirective();
    expect(directive).toBeTruthy();
  });
});
